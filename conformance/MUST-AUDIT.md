# The D-104 MUST audit — MLP/0.1 draft-03

Generated by `audit-musts.py` + `audit-annotate.py`; CI regenerates
both and fails on drift, so a spec edit that adds a requirement
forces an audit decision before merge.

**75 corpus entries.** Of the 70 testable requirements: **50 COVERED** by failing-input or vector tests, 2 PARTIAL, 7 OPEN-CLIENT (presentation-layer, S3.10/S3.11 backlog per D-233), 11 OPEN-DEFERRED (unimplemented optional machinery, each tied to its decision).

| # | Section | Requirement | Status | Coverage |
|---|---------|-------------|--------|----------|
| M001 | 2.1 Requirement language | The key words "MUST", "MUST NOT", "REQUIRED", "SHALL", "SHALL NOT", "SHOULD", | **META** | RFC 2119 boilerplate (§2.1). |
| M002 | 2.3 JSON conventions | 1. **Numbers.** All numeric values MUST be integers. Floating-point values | **COVERED** | sn/must_test.go TestJSONConventionRefusals/float_manifest_size |
| M003 | 2.3 JSON conventions | MUST NOT appear anywhere in MLP JSON. Every integer MUST satisfy | **COVERED** | same case: a float anywhere in a validated document refuses; core/tv001_test.go TestDialectRejections guards the parser |
| M004 | 2.3 JSON conventions | designator (e.g., `2026-07-04T12:00:00Z`). Epoch numbers MUST NOT be used. | **COVERED** | sn/must_test.go TestJSONConventionRefusals/epoch_timestamp |
| M005 | 2.3 JSON conventions | 5. **Unknown members** MUST be ignored on read (forward compatibility) but are | **COVERED** | sn/must_test.go TestUnknownMemberTolerance (the positive tolerance case); discovery TestUnknownMembersIgnored |
| M006 | 2.4 Signing conventions (overview) | Creators SHOULD emit payloads in JCS form on the wire; verifiers MUST | **COVERED** | core/tv001_test.go TestTV001 (canonicalize-then-verify against the vector); sn TestVerifyTV002Verdicts |
| M007 | 3.1 Overview | **Optional members are omitted when absent.** The value `null` MUST NOT be | **COVERED** | sn/must_test.go TestJSONConventionRefusals/null_subject + null_envelope_to_entry |
| M008 | 3.1 Overview | Receivers MUST ignore unknown members (§2.3, rule 5); where a document is | **COVERED** | as M005 |
| M009 | 3.2.1 Fields | from `addr` MUST trigger caution UI). | **OPEN-CLIENT** | display-name caution UI (D-164); client presentation backlog, S3.10/D-233 |
| M010 | 3.2.2 Manifest Entries | `urn` \| string \| REQUIRED \| The Media object's content address (`urn:mlet:`, D-25). Entries MUST have disti… | **COVERED** | sn/must_test.go TestJSONConventionRefusals/duplicate_manifest_urn |
| M011 | 3.2.2 Manifest Entries | `name` \| string \| OPTIONAL \| Display filename, 1–255 code points. A display string only: receivers MUST NOT… | **OPEN-CLIENT** | name-is-not-a-path neutralization happens at client save (D-47); server never treats name as a path (no path use exists — grep-verifiable), client save-dialog gate open |
| M012 | 3.2.2 Manifest Entries | `preview_of` \| string \| OPTIONAL \| The `urn` of another entry **in the same Manifest** for which this entry… | **COVERED** | sn/mep_test.go TestTV007PreviewOfValidation (all four violating shapes) |
| M013 | 3.2.3 Constraints | 2. Every `urn:mlet:` reference appearing in the Body MUST correspond to a | **COVERED** | render TestGoSanitizerReproducesTV005 + client run-tv005.js: unmanifested references removed (corpus cases) |
| M014 | 3.3.2 Author Signature construction | members (verifiers MUST canonicalize-then-verify, D-44); resolve | **COVERED** | as M006 |
| M015 | 3.4.1 Fields | `envelope_to` \| array of string \| REQUIRED \| Non-empty array of bare Addresses (no display names — routing … | **COVERED** | sn TestValidationFailureMatrix/mixed_domains |
| M016 | 3.4.1 Fields | `fulfillment_sources` \| array of object \| OPTIONAL \| Each `{ "domain": <string, REQUIRED>, "urns": <array o… | **COVERED** | sn/mep_test.go (TV-006 suite): strict until validation; malformed until = malformed envelope |
| M017 | 3.4.2 The hop chain | The receiving SN MUST fully verify the **current** Hop Signature (it holds | **COVERED** | sn TestValidationFailureMatrix/hop_sig_tamper; forward tests verify the current hop |
| M018 | 3.4.2 The hop chain | **Loop prevention (D-51).** An SN MUST NOT automatically re-dispatch | **COVERED** | sn/forward_test.go: automatic re-dispatch into the chain refused (D-51) |
| M019 | 3.4.4 Caps and validation at receipt | The complete Signed Envelope document, as transmitted in UTF-8, MUST NOT | **COVERED** | sn TestValidationFailureMatrix/oversized |
| M020 | 3.4.4 Caps and validation at receipt | 3. `envelope_to` locality: every Address MUST be at a domain this SN serves; | **COVERED** | sn TestValidationFailureMatrix/non-local_recipient |
| M021 | 3.5 The Delivery Record | implementation-free, but for each delivered Medialet the receiving SN MUST | **COVERED** | sn materialization asserted across the suite (threads/messages/refs after ingest); cmd/mlpd TestTwoDomainDemo end to end |
| M022 | 4.1 Address grammar | 4. `domain` MUST contain at least one `.` (single-label names do not | **COVERED** | sn/must_test.go TestAddressGrammarRefusals (single-label + six more violations) |
| M023 | 4.3 Internationalized domains | (Punycode, RFC 5890–5893). Implementations processing user input MUST apply | **PARTIAL** | the server enforces the LDH + hyphen-position subset (TestAddressGrammarRefusals, the audit-found underscore fix); full IDNA2008 U-label conversion is user-input handling in clients (§4.3) — untested there |
| M024 | 4.4 Display safety | Tier 1 correspondent, clients MUST either render the domain in A-label | **OPEN-CLIENT** | A-label rendering rule for non-correspondents (D-164); client backlog |
| M025 | 4.4 Display safety | accompanying `addr`, clients MUST NOT render the name as if it were the | **OPEN-CLIENT** | display-name-as-address refusal; client backlog |
| M026 | 4.4 Display safety | address and MUST apply caution UI (show the real `addr` prominently). | **OPEN-CLIENT** | caution UI; client backlog |
| M027 | 5.1 Overview and algorithm | DNSSEC-validated MUST NOT substitute for step 2; it MAY be used only to | **OPEN-DEFERRED** | the DNS hint path (§5.3) is not implemented in the reference — HTTPS-only discovery; the MUST binds implementations that process hints (D-12). Deferred with §5.3. |
| M028 | 5.1 Overview and algorithm | 3. **Validation.** The response MUST be a valid Domain Document (5.2) whose | **COVERED** | discovery TestDomainBindingHardFails / TestVersionIntersectionHardFails / TestMissingRequiredMemberHardFails |
| M029 | 5.1 Overview and algorithm | document and a DNSSEC-validated hint-path document for `D`, they MUST | **OPEN-DEFERRED** | hint/HTTPS disagreement hard-fail: as M027, deferred with the hint machinery |
| M030 | 5.2 The Domain Document | `domain` \| string \| REQUIRED \| The domain this document is authoritative for, lowercase A-labels. Resolvers… | **COVERED** | discovery TestDomainBindingHardFails (the D-57 binding check) |
| M031 | 5.2 The Domain Document | `capabilities` \| array of string \| OPTIONAL \| Optional-capability tokens this domain's endpoints support, f… | **OPEN-DEFERRED** | the `capabilities` member (MEP-003) is not parsed by discovery yet — lands with the bao implementation substage (D-269 notes the dependency); the tolerance machinery itself is the M005 family |
| M032 | 5.2 The Domain Document | `not_after` \| string \| OPTIONAL \| RFC 3339 UTC validity end. Keys outside their window MUST be rejected for… | **COVERED** | discovery TestVerificationKeySemantics (windows); bs TestFreshnessWindowAndRoles for transfer keys |
| M033 | 5.3 The DNS hint | `v` is REQUIRED and MUST be `MLP1` for this specification. `url`, when | **OPEN-DEFERRED** | §5.3 hint grammar (v=MLP1): as M027 |
| M034 | 5.3 The DNS hint | present, MUST be `https` and points at the Domain Document. A record without | **OPEN-DEFERRED** | §5.3 hint url https: as M027 |
| M035 | 5.4 The hardened fetch profile | 2. Response size cap 65,536 bytes — connections MUST be aborted beyond it. | **COVERED** | discovery TestFetchSizeCapAborts |
| M036 | 5.4 The hardened fetch profile | Implementations MUST pin the resolved address for the actual connection | **COVERED** | discovery TestDialTimeAddressCheckWired (resolve-then-pin at connect time) |
| M037 | 5.5 Caching and key freshness | MUST re-fetch upon encountering an unknown `kid`. Failed discovery SHOULD be | **COVERED** | discovery TestResolverCacheCeilingAndKidRefetch (unknown-kid refetch) |
| M038 | 5.6 Per-user resolution (optional) | peers, and MUST rate-limit it if enabled; the anti-enumeration rationale is | **PARTIAL** | per-user resolution (§5.6) is not implemented (optional); the rate-limit MUST binds enablers — recorded here so enabling it forces the test |
| M039 | 6.1 Key material and encoding | fields. On loading any Key Entry, verifiers MUST check that the multicodec | **COVERED** | discovery TestAlgMismatchIgnoresEntry (multicodec/alg agreement) |
| M040 | 6.2 Key identifiers | is normative: on loading a key set, implementations MUST recompute each | **COVERED** | discovery TestKidSelfVerificationIgnoresEntry |
| M041 | 6.2 Key identifiers | entry's kid from its `key` member and MUST ignore entries whose published | **COVERED** | discovery TestKidSelfVerificationIgnoresEntry + TestDuplicateKidIgnored |
| M042 | 6.4 Document signatures | MUST NOT implement the primitives themselves. | **META** | MUST NOT implement primitives yourself — a build/dependency policy (stdlib crypto only; go.mod is the witness), not input-testable |
| M043 | 6.6 HTTP message signatures (transfer profile) | `alg` MAY be present and MUST then be `ed25519`. Receivers MUST enforce | **COVERED** | bs TestFreshnessWindowAndRoles (alg enforcement + windows on transfer signatures) |
| M044 | 7.3 Dispatch processing | recorded dispatch MUST be answered with the current verdict snapshot | **COVERED** | sn TestRetryIdempotencyAndReplay (recorded dispatch answered with the current snapshot) |
| M045 | 7.4 The verdict document | entry MUST appear exactly once in `recipients`. | **COVERED** | sn TestUnknownRecipientAndCompleteness (each envelope_to entry exactly once) |
| M046 | 7.4 The verdict document | behavior, not a bypass. Every Manifest `urn` MUST appear exactly once in | **COVERED** | sn TestUnknownRecipientAndCompleteness (every Manifest urn exactly once in media) |
| M047 | 7.5 Reservations | redirect, which pushers SHOULD refuse entirely — the pushing BS MUST apply | **COVERED** | bs TestHardenedPusherRefusesLoopbackAndHTTP (D-72 on the pushing side) |
| M048 | 7.5 Reservations | resolve-then-pin) and MUST require `https`. Without this, a malicious | **COVERED** | sn verdict parsing refuses non-https reservation target_url (§7.5); exercised by the S4.13 AllowInsecureTransport branch in reverse — the production default path is the refusal, cmd/mlpd tests run the demo branch and sn/verdict tests the default |
| M049 | 7.6 Verdict updates | `have`, `deny` \| — \| Terminal; updates MUST NOT alter them. \| | **COVERED** | sn TestOriginTransitionWalk + store TestReplayUniqueAndReservationTerminal (terminal verdicts immutable) |
| M050 | 7.7 Default acceptance policy | Mechanics above are MUST-grade; the policy that drives them is | **META** | the §7.7 mechanics/policy split statement; the mechanics MUSTs are audited individually above |
| M051 | 8.2 The upload resource | 6. Pushers MUST apply the connection-safety rules of D-72 (§7.5) and | **COVERED** | as M047 (the §8.2 restatement of D-72) |
| M052 | 8.4 PATCH — pushing bytes | request's body, RFC 9530), the token header, and the signature. `N` MUST | **COVERED** | bs TestOffsetMismatchAndHashReset + TestDigestMismatchRollsBack (the PATCH N rules) |
| M053 | 8.6 Object verification, segments, and quarantine | array length MUST equal `ceil(size / 16 MiB)` or the entry is malformed. | **OPEN-DEFERRED** | segments (§8.6) are unimplemented in the reference BS (single-digest verification only); the length-equality MUST lands with segment support. Recorded as the largest deferred wire feature. |
| M054 | 8.9 Verified-streaming push: mlp-bao (MEP-003) | deterministic function of the content length; Annex D). A pusher MUST | **OPEN-DEFERRED** | §8.9 pusher-side advertisement gate; bao implementation substage (D-269) |
| M055 | 8.9 Verified-streaming push: mlp-bao (MEP-003) | MUST reject a non-verifying node or group with 422 `bao-verify-failed` | **OPEN-DEFERRED** | §8.9 receiver-side incremental rejection; TV-008 corrupted_slice is the prepared failing input; bao implementation substage (D-269) |
| M056 | 9.2 Constructing a forwarded dispatch | 1. **Chain integrity** (D-84). The forwarder MUST carry the complete | **COVERED** | sn/forward_test.go TestForwardReproducesTV004Envelope (complete chain carried) |
| M057 | 9.2 Constructing a forwarded dispatch | forwarder MUST list at least the custody-holding sources it knows, the | **COVERED** | forward tests: the root origin guaranteed present in sources |
| M058 | 9.2 Constructing a forwarded dispatch | domain MUST be a chain member — the `origin` of the dispatched Envelope | **COVERED** | sn/must_test.go TestNonChainSourceNeverContacted (receiver side) |
| M059 | 9.2 Constructing a forwarded dispatch | or of a Hop Attestation in it; receivers MUST ignore listed sources | **COVERED** | same test: the interloper is ignored, the chain member still asked |
| M060 | 10.4 Tombstones | The `unavailable` record MUST retain: `urn`, declared `size`, `type`, | **COVERED** | sn/gc_test.go TestGCInvariants (the tombstone minimum record survives the flip) |
| M061 | 10.4 Tombstones | and the transition timestamp (D-87). Clients MUST render tombstones | **OPEN-CLIENT** | tombstone rendering; client backlog |
| M062 | 10.5 Retention, pinning, and garbage collection | 1. An object MUST be retained while any local reference to it is | **COVERED** | sn/gc_test.go TestGCInvariants (pinned retains absolutely — §10.5 invariant 1) |
| M063 | 10.7 Resolution semantics and access control | Given an authenticated mailbox owner and a URN, the home side MUST answer | **COVERED** | clientapi media tests: recipients-only raw endpoint + hardened object serving (S4.11); §10.7 access answered for owners |
| M064 | 11.2 Elements | `loop`, `preload`, and `controls` are stripped, and clients MUST present | **COVERED** | render/client TV-005 corpus: media element attribute stripping |
| M065 | 11.2 Elements | playable media with user-facing controls and MUST NOT autoplay. | **OPEN-CLIENT** | no-autoplay presentation; the sanitizer strips autoplay (TV-005) — the play-with-controls duty is presentation |
| M066 | 11.4 URLs and references | (inserted empty when missing). Clients MUST open external links with | **COVERED** | client TV-005 + viewer: external links noopener/noreferrer with title disclosure (run-html.js discipline + viewer tests) |
| M067 | 11.5 The sanitization pipeline | Clients render the derived form and MUST sanitize again at render time | **COVERED** | client run-tv005.js: render-time re-sanitization is the pipeline under test (D-31 dual duty) |
| M068 | 11.5 The sanitization pipeline | 4. **Idempotence is REQUIRED**: sanitize(sanitize(B)) MUST equal | **COVERED** | TV-005 idempotence: both implementations, all 14 cases (sanitize∘sanitize = sanitize) |
| M069 | 11.7 Client floor | Web clients MUST render Bodies inside an isolated DOM (shadow root or | **COVERED** | client viewer: shadow-DOM isolation (mlp-body-viewer tests; the §11.7 floor) |
| M070 | Annex B (informative): Deployment Topologies | abort, transactional verification (§8) — MUST front it; a bare bucket | **COVERED** | bs transactional verification fronts the store (TestTranscriptWalk, TestRestartRederivation); §14.3 restates §8 |
| M071 | Annex C (informative): Conformance Overview | **every MUST in this specification maps to at least one test whose input | **META** | the D-104 principle itself |
| M072 | Annex C (informative): Conformance Overview | violates it** — a MUST without a failing-input test is a MUST on the | **META** | the D-104 principle, continued |
| M073 | Annex D (normative): The application/mlp-bao Encoding | Encoders MUST emit full 16,384-byte groups except the final group, | **OPEN-DEFERRED** | Annex D geometry; TV-008 pins it byte-exactly (generator self-checks); Go emitter pending (D-269) |
| M074 | Annex D (normative): The application/mlp-bao Encoding | The topmost value, root-finalized, MUST equal the URN's digest — | **OPEN-DEFERRED** | Annex D root==URN; enforced today for the vector emitter by TV-008 self-check (root equals the independent blake3 of the content); implementation-side test pending (D-269) |
| M075 | Annex D (normative): The application/mlp-bao Encoding | accepting its bytes; verifiers MUST NOT release or act on any byte of | **OPEN-DEFERRED** | Annex D no-release-before-verify; the TV-008 corrupted_slice expectation states it; implementation-side test pending (D-269) |
