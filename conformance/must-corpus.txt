# 69 lines, 75 MUST/MUST NOT occurrences — generated by audit-musts.py; do not edit
M001 [2.1 Requirement language] (2) The key words "MUST", "MUST NOT", "REQUIRED", "SHALL", "SHALL NOT", "SHOULD",
M002 [2.3 JSON conventions] (1) 1. **Numbers.** All numeric values MUST be integers. Floating-point values
M003 [2.3 JSON conventions] (2) MUST NOT appear anywhere in MLP JSON. Every integer MUST satisfy
M004 [2.3 JSON conventions] (1) designator (e.g., `2026-07-04T12:00:00Z`). Epoch numbers MUST NOT be used.
M005 [2.3 JSON conventions] (1) 5. **Unknown members** MUST be ignored on read (forward compatibility) but are
M006 [2.4 Signing conventions (overview)] (1) Creators SHOULD emit payloads in JCS form on the wire; verifiers MUST
M007 [3.1 Overview] (1) **Optional members are omitted when absent.** The value `null` MUST NOT be
M008 [3.1 Overview] (1) Receivers MUST ignore unknown members (§2.3, rule 5); where a document is
M009 [3.2.1 Fields] (1) from `addr` MUST trigger caution UI).
M010 [3.2.2 Manifest Entries] (1) `urn` | string | REQUIRED | The Media object's content address (`urn:mlet:`, D-25). Entries MUST have distinct `urn` values. |
M011 [3.2.2 Manifest Entries] (2) `name` | string | OPTIONAL | Display filename, 1–255 code points. A display string only: receivers MUST NOT interpret it as a filesystem path; path separators and traversal sequences carry no meaning and clients MUST neutralize them on save (D-47). |
M012 [3.2.2 Manifest Entries] (2) `preview_of` | string | OPTIONAL | The `urn` of another entry **in the same Manifest** for which this entry is a reduced-fidelity preview (MEP-002). Constraints: the referenced `urn` MUST be present in the Manifest; an entry carrying `preview_of` MUST NOT itself be the target of any `preview_of` (no chains); self-reference is forbidden. A violating member is **ignored** at ingest validation (the entry otherwise stands). Purely descriptive — never a policy input (D-111/D-107 inherited; auto-grant continues to key on `size` alone, D-139). |
M013 [3.2.3 Constraints] (1) 2. Every `urn:mlet:` reference appearing in the Body MUST correspond to a
M014 [3.3.2 Author Signature construction] (1) members (verifiers MUST canonicalize-then-verify, D-44); resolve
M015 [3.4.1 Fields] (1) `envelope_to` | array of string | REQUIRED | Non-empty array of bare Addresses (no display names — routing data only). All entries MUST share a single domain: the target domain. Maximum **128** entries (D-52). Bcc semantics: one Envelope per Bcc recipient, naming only that recipient, even when several Bcc recipients share a domain (D-03). |
M016 [3.4.1 Fields] (1) `fulfillment_sources` | array of object | OPTIONAL | Each `{ "domain": <string, REQUIRED>, "urns": <array of string, OPTIONAL — absent means all Manifest URNs>, "until": <string, OPTIONAL, RFC 3339 UTC — MEP-001> }`, in preference order (nearest hop first, D-24). `until` is the declaring source's own offer window for the URNs this entry covers: its promise that it will honor grants (as enveloping origin, §7.6) or delegation requests (§9.4) for those objects until the stated time. Absent means the single source `origin` — the direct-dispatch case. An SN dispatching a delegated forward MUST list at least the custody-holding sources it knows, the root origin at minimum (D-22–D-24). |
M017 [3.4.2 The hop chain] (1) The receiving SN MUST fully verify the **current** Hop Signature (it holds
M018 [3.4.2 The hop chain] (1) **Loop prevention (D-51).** An SN MUST NOT automatically re-dispatch
M019 [3.4.4 Caps and validation at receipt] (1) The complete Signed Envelope document, as transmitted in UTF-8, MUST NOT
M020 [3.4.4 Caps and validation at receipt] (1) 3. `envelope_to` locality: every Address MUST be at a domain this SN serves;
M021 [3.5 The Delivery Record] (1) implementation-free, but for each delivered Medialet the receiving SN MUST
M022 [4.1 Address grammar] (1) 4. `domain` MUST contain at least one `.` (single-label names do not
M023 [4.3 Internationalized domains] (1) (Punycode, RFC 5890–5893). Implementations processing user input MUST apply
M024 [4.4 Display safety] (1) Tier 1 correspondent, clients MUST either render the domain in A-label
M025 [4.4 Display safety] (1) accompanying `addr`, clients MUST NOT render the name as if it were the
M026 [4.4 Display safety] (1) address and MUST apply caution UI (show the real `addr` prominently).
M027 [5.1 Overview and algorithm] (1) DNSSEC-validated MUST NOT substitute for step 2; it MAY be used only to
M028 [5.1 Overview and algorithm] (1) 3. **Validation.** The response MUST be a valid Domain Document (5.2) whose
M029 [5.1 Overview and algorithm] (1) document and a DNSSEC-validated hint-path document for `D`, they MUST
M030 [5.2 The Domain Document] (1) `domain` | string | REQUIRED | The domain this document is authoritative for, lowercase A-labels. Resolvers MUST verify it equals the queried domain. Prevents cross-domain document confusion, notably at multi-tenant providers. (D-57) |
M031 [5.2 The Domain Document] (1) `not_after` | string | OPTIONAL | RFC 3339 UTC validity end. Keys outside their window MUST be rejected for verification of newly received material; stored-mail semantics follow verification-at-ingest (D-32). |
M032 [5.3 The DNS hint] (1) `v` is REQUIRED and MUST be `MLP1` for this specification. `url`, when
M033 [5.3 The DNS hint] (1) present, MUST be `https` and points at the Domain Document. A record without
M034 [5.4 The hardened fetch profile] (1) 2. Response size cap 65,536 bytes — connections MUST be aborted beyond it.
M035 [5.4 The hardened fetch profile] (1) Implementations MUST pin the resolved address for the actual connection
M036 [5.5 Caching and key freshness] (1) MUST re-fetch upon encountering an unknown `kid`. Failed discovery SHOULD be
M037 [5.6 Per-user resolution (optional)] (1) peers, and MUST rate-limit it if enabled; the anti-enumeration rationale is
M038 [6.1 Key material and encoding] (1) fields. On loading any Key Entry, verifiers MUST check that the multicodec
M039 [6.2 Key identifiers] (1) is normative: on loading a key set, implementations MUST recompute each
M040 [6.2 Key identifiers] (1) entry's kid from its `key` member and MUST ignore entries whose published
M041 [6.4 Document signatures] (1) MUST NOT implement the primitives themselves.
M042 [6.6 HTTP message signatures (transfer profile)] (2) `alg` MAY be present and MUST then be `ed25519`. Receivers MUST enforce
M043 [7.3 Dispatch processing] (1) recorded dispatch MUST be answered with the current verdict snapshot
M044 [7.4 The verdict document] (1) entry MUST appear exactly once in `recipients`.
M045 [7.4 The verdict document] (1) behavior, not a bypass. Every Manifest `urn` MUST appear exactly once in
M046 [7.5 Reservations] (1) redirect, which pushers SHOULD refuse entirely — the pushing BS MUST apply
M047 [7.5 Reservations] (1) resolve-then-pin) and MUST require `https`. Without this, a malicious
M048 [7.6 Verdict updates] (1) `have`, `deny` | — | Terminal; updates MUST NOT alter them. |
M049 [7.7 Default acceptance policy] (1) Mechanics above are MUST-grade; the policy that drives them is
M050 [8.2 The upload resource] (1) 6. Pushers MUST apply the connection-safety rules of D-72 (§7.5) and
M051 [8.4 PATCH — pushing bytes] (1) request's body, RFC 9530), the token header, and the signature. `N` MUST
M052 [8.6 Object verification, segments, and quarantine] (1) array length MUST equal `ceil(size / 16 MiB)` or the entry is malformed.
M053 [9.2 Constructing a forwarded dispatch] (1) 1. **Chain integrity** (D-84). The forwarder MUST carry the complete
M054 [9.2 Constructing a forwarded dispatch] (1) forwarder MUST list at least the custody-holding sources it knows, the
M055 [9.2 Constructing a forwarded dispatch] (1) domain MUST be a chain member — the `origin` of the dispatched Envelope
M056 [9.2 Constructing a forwarded dispatch] (1) or of a Hop Attestation in it; receivers MUST ignore listed sources
M057 [10.4 Tombstones] (1) The `unavailable` record MUST retain: `urn`, declared `size`, `type`,
M058 [10.4 Tombstones] (1) and the transition timestamp (D-87). Clients MUST render tombstones
M059 [10.5 Retention, pinning, and garbage collection] (1) 1. An object MUST be retained while any local reference to it is
M060 [10.7 Resolution semantics and access control] (1) Given an authenticated mailbox owner and a URN, the home side MUST answer
M061 [11.2 Elements] (1) `loop`, `preload`, and `controls` are stripped, and clients MUST present
M062 [11.2 Elements] (1) playable media with user-facing controls and MUST NOT autoplay.
M063 [11.4 URLs and references] (1) (inserted empty when missing). Clients MUST open external links with
M064 [11.5 The sanitization pipeline] (1) Clients render the derived form and MUST sanitize again at render time
M065 [11.5 The sanitization pipeline] (1) 4. **Idempotence is REQUIRED**: sanitize(sanitize(B)) MUST equal
M066 [11.7 Client floor] (1) Web clients MUST render Bodies inside an isolated DOM (shadow root or
M067 [14.3 Versioning policy] (1) abort, transactional verification (§8) — MUST front it; a bare bucket
M068 [14.3 Versioning policy] (1) **every MUST in this specification maps to at least one test whose input
M069 [14.3 Versioning policy] (2) violates it** — a MUST without a failing-input test is a MUST on the
